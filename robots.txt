# App Lançamentos (raiz) é público -- indexação normal.
# /painel/ é o sistema administrativo interno -- fora de indexação (a
# proteção de acesso real continua sendo o login, isso aqui só evita que
# buscadores/bots bem-comportados listem/rastreiem as URLs do painel).
User-agent: *
Disallow: /painel/
